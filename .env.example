POSTGRES_USER=tracker
POSTGRES_PASSWORD=changeme
POSTGRES_DB=workout_tracker
DATABASE_URL=postgresql://tracker:changeme@db:5432/workout_tracker
# Leave blank to auto-derive from whatever host the page loads from (localhost, LAN IP,
# workout.lan, etc.), on port 8001. Only set this explicitly if the API is served from a
# different host than the frontend (e.g. separate NPM hostnames in production).
VITE_API_URL=
